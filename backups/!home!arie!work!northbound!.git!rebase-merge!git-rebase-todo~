pick b0b5c73 BK-2672: Add reboot action
pick cbbf94f BK-3419: Added support for both query params and multiple params of the same name
pick 455b810 added tag support to volumes, snapshots and images; finished it for VMs
pick cab042c vm create: tags are now a list, not a CSV
pick e3298cb BK-2668: move old iodocs generation code from mui
pick 5a4e695 BK-2668: iodocs config moved to strato-iodocs
pick 5cd455a BK-2668: add uri prefix for compatibility

# Rebase 98c1ec4..5cd455a onto 98c1ec4 (7 command(s))
#
# Commands:
# p, pick = use commit
# r, reword = use commit, but edit the commit message
# e, edit = use commit, but stop for amending
# s, squash = use commit, but meld into previous commit
# f, fixup = like "squash", but discard this commit's log message
# x, exec = run command (the rest of the line) using shell
#
# These lines can be re-ordered; they are executed from top to bottom.
#
# If you remove a line here THAT COMMIT WILL BE LOST.
#
# However, if you remove everything, the rebase will be aborted.
#
# Note that empty commits are commented out
